function agent = agentTd3Residual7250(observationInfo, actionInfo)
%agentTd3Residual7250 keeps backward compatibility with the historical name.
agent = agentTd3ResidualLift(observationInfo, actionInfo);
end
