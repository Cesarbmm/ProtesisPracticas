# Reward Functions

Carpeta estable de rewards.

## Estado actual

La migracion desde la carpeta con espacio ya fue realizada y el repo resuelve estas funciones via `addpath(genpath(...))`.

## Regla

- mantener este nombre como ruta definitiva
- no recrear una carpeta `reward functions/`
- si se agregan rewards nuevas, deben vivir aqui
