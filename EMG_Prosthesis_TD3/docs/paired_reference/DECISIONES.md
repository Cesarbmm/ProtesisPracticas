# Decisiones

Una fila por etapa. El veredicto se escribe **después** de ver los resultados, contra el umbral que
el preregistro fijó **antes**.

| Etapa | Fecha | Veredicto | Motivo |
|---|---|---|---|
| E0 | 2026-09-04 | REGISTRADA | Preregistro escrito y entregables listos. Pendiente de ejecución en MATLAB. |
| E0 | 2026-09-04 | **CERRADA (operativo)** | `GATE_GLOBAL_ORIGINAL = FAIL` se conserva; `GATE_OPERATIVO = PASS` se anade por Enmienda 2, autorizada por Cesar y decidida DESPUES de ver los datos. Split congelado por Enmienda 3. Falta unicamente el manifiesto de toolboxes. Ver `04_CIERRE_E0.md`. |
| E1C | 2026-09-04 | **STOPPED_AFTER_ORACLE** | `ORACLE_ALIGNMENT_POTENTIAL = NO`. El oraculo de 8 desplazamientos da +3.5 % con lambda modal y +3.3 % en transferencia por mitades, no el +15.5 % nominal, que era una patologia de seleccion de lambda en el fold de DENIS. El desplazamiento es estable por sujeto pero no coincide entre gestos (2/10) ni entre motores (0/10): no se comporta como una rotacion del brazalete. Paro forzado: el selector EMG-only no puede superar el techo de +3.3 % frente a un gate del 15 %. Ver `08_RESULTADOS_E1C.md`. |
| E1C | 2026-09-04 | REGISTRADA | Preregistro con verificacion 5.1 bloqueante del recalculo de features fuera de MATLAB. Enmienda 1 (criterio absoluto) y Enmienda 2 (anulacion del aprobado nominal), ambas fechadas y posteriores a los datos. |
| E1B | 2026-09-04 | **FAIL — `E1B_EMG_ONLY_CALIBRATION_FAIL`** | 0 de 8 condiciones. Calibrado 0.039702 frente a sin calibrar 0.040639 (+2.3 %; anidada +7.6 %), umbral 15 %. Frente a la media 0.041368 solo +4.0 %, umbral 20 %. 6/10 sujetos, 1/4 motores, sign acc empeora 0.539 -> 0.531, shuffle global +4.3 %, condicionado +0.6 %. TEST no ejecutado. Ver `07_RESULTADOS_E1B.md`. |
| E1B | 2026-09-04 | REGISTRADA | Preregistro escrito antes de ajustar nada, con umbrales fijados de antemano. Se anadio la condicion 2 (mejora >=20 % sobre la media) y se justifico. Ver `PREREGISTRO_E1B.md`. |
| E1 | 2026-09-04 | **PARA — `E1_SUBJECT_SPECIFIC_ONLY`** | Gate fallido en 6 de 7 condiciones. ridge40 val MSE 0.052142 frente a baseline B 0.027180 y baseline A 0.016095; 0 de 4 motores a favor. Entre sujetos la ablacion condicionada da -2.7% (barajar la EMG mejora). Dentro de sujeto la senal es real: +80.9% global y +6.4% condicionada, positiva en 8/8. Test NO ejecutado. Ver `06_RESULTADOS_E1.md`. |
| E1 | 2026-09-04 | REGISTRADA | Preregistro escrito antes de ajustar ningun modelo. Export del dataset pendiente de ejecucion en MATLAB. Ver `PREREGISTRO_E1.md`. |
| E0 | 2026-09-04 | PARA Y CORRIGE | Gate fallido: 174 fallos de monotonicidad y 6 saltos en 2352 casos. Causa raíz con correlación 1:1: el arranque al final de la curva cuando la búsqueda de `x_0` no encuentra punto. Fix mínimo aplicado. Pendiente de re-ejecución. Ver `03_RESULTADOS_E0.md`. |

## Decisiones previas a la apertura de la línea

| Fecha | Decisión | Motivo |
|---|---|---|
| 2026-09-04 | No se pasa a DQN / Double DQN / Branching | El espacio `{-64,0,+64}` que motivaba el cambio era consecuencia de `actionCommandScale = 0.2510` impuesto en ETAPA 10, no una propiedad de la planta. En `main` cada motor distingue 15 comandos (`actionCommandLevels = [0 64 96 128 160 192 224 255]`, `speeds = 255`). |
| 2026-09-04 | Se abre rama nueva desde `main` en vez de continuar `experiment/no-glove-intent-control` | 88 commits, 40 workflows y 40 tests, con la secuencia `07a`…`07u` como evidencia de deriva de hipótesis. |
| 2026-09-04 | El guante pasa a ser maestro offline, no sensor de ejecución | En `main`, `src/@Env/step.m:123-124` construye la referencia como `flexJoined_scaler(reduceFlexDimension(flexData))`. El guante nunca entró en `markov52`. Sustituir esa función es todo el cambio necesario. |
| 2026-09-04 | Alcance limitado a OPENING / CLOSING | Es lo único que el dataset Denis contiene. |

## Hallazgos técnicos abiertos

| Fecha | Hallazgo | Estado |
|---|---|---|
| 2026-09-04 | `ws` es un objeto `cfit` cuyo `numel` valdría 1, colapsando la trayectoria a una constante. | **REFUTADO** por E0: MATLAB lee las 56 entradas como numéricas vacías. Ver la nota en `02_HALLAZGO_PLANTA.md`. |
| 2026-09-04 | La física de la planta **depende de las toolboxes instaladas**: `fit_C2.mat` guarda objetos `cfit`, y sin Curve Fitting Toolbox MATLAB los carga como `[]`, activando el respaldo a `pattern_curve`. Con la toolbox, `numel(ws)=1` y la trayectoria colapsa. `Agent7250` y todas las campañas se entrenaron en una de las dos ramas y no está registrado en cuál. | ABIERTO. Confirmar con `ver` si Curve Fitting Toolbox está instalada. Registrar la configuración de toolboxes junto a cada resultado. |
| 2026-09-04 | Arranque al final de la curva cuando la posición queda fuera del recorrido: 73 de 1176 casos del paso operativo se mueven en dirección contraria al comando. Correlación 1:1 con los fallos de la búsqueda de `x_0`. | **CONFIRMADO y CORREGIDO.** Fix mínimo en `prosthesis_simulator.m` + test de regresión. Pendiente de re-ejecutar E0. |
| 2026-09-04 | Resolución de control baja: un paso de 0.2 s cubre del 12.7 % al 47.2 % del recorrido. A PWM >= 128 hay poco control proporcional; en la mitad de las combinaciones de los motores 2 y 4 todas las posiciones iniciales terminan en el mismo sitio. Los agentes históricos operaban con `saturationFraction` 0.39-0.61, es decir en ese régimen. | ABIERTO. No es un bug del simulador: es física. Se decide en el diseño del experimento (periodo de control o niveles permitidos), no parcheando el simulador. |
| 2026-09-04 | La curva de referencia de motor 3 en cierre no es monótona. 28 fallos residuales, sólo en el caso de estrés de 3.0 s, nunca en el paso operativo. | A `BACKLOG.md`. Defecto del dato de caracterización. |
| 2026-09-04 | `normValues.mat` contiene el workspace completo con el que se calcularon C y S: `F` de 40 x 45900, `featuresData` con `user1`..`user306` y 6 gestos. 306 x 6 x 25 = 45900. C y S NO salieron del dataset Denis de 12 sujetos, sino de un corpus de 306 sujetos y 6 gestos. El solapamiento de sujetos es inverificable (identificadores anonimos). | `PREPROCESSING_LEAKAGE_STATUS = POSSIBLE_LEAKAGE`, etiqueta conservadora. C y S **no** se recalculan. Comprobacion numerica pendiente en E1. Ver `05_AUDITORIA_NORMALIZACION.md`. |
| 2026-09-04 | Curve Fitting Toolbox fue instalada **despues** de ejecutar E0. Los numeros de E0 corresponden a un entorno sin la toolbox (`ws` leido como vacio, rama `pattern_curve`). Con la toolbox, `numel(ws) = 1` y el clamp deja la trayectoria constante. | ABIERTO. No re-ejecutar E0 sin fijar antes el contrato. `CANONICAL_PLANT_SOURCE_NEW_LINE = pattern_curve.mat` (MD5 `3fd6ff43...`). Cambio de codigo de una linea **demostrado y NO aplicado** en `04_CIERRE_E0.md` seccion 3.6. |
| 2026-09-04 | Auditoria del export: 12 sujetos, split correcto, 7420 episodios, 113057 pasos, phi=40 y target=4, sin `Env`/`SimController`/agente. Las 40 features validadas contra `fGetFeatures` sobre la misma EMG con error relativo maximo de 6.1e-14. Perdida por \|k\|: 6.56% y 13.13%. | CERRADO, artefacto valido. |
| 2026-09-04 | Comprobacion de `05_AUDITORIA_NORMALIZACION` seccion 6: los bloques F2 y F5, que escalan con la longitud de ventana, difieren 17x y 33x entre los C/S almacenados y los calculados con Denis. Evidencia estructural de que C/S vienen de otro corpus y otra ventana. | La etiqueta `POSSIBLE_LEAKAGE` **se conserva por instruccion**. El cambio a `KNOWN_CLEAN` es decision de Cesar. |
| 2026-09-04 | `WMoos_F2` aplica `hilbert` y `sgolayfilt` a lo largo de los 8 canales, no del tiempo (MATLAB opera por columnas sobre `emg'` de 8xm). Casi seguro no intencionado. | DOCUMENTADO, **no corregido**, por instruccion. |
| 2026-09-04 | La hipotesis de *domain shift* afin EMG-only queda **refutada**: alinear media y varianza de las 40 features por sujeto, con hasta 122 s de EMG de calibracion, no recupera la transferencia. La curva de presupuesto es plana (12 s -> +0.6 %, 122 s -> +2.3 %). | CERRADO. Candidato restante: la relacion EMG->guante difiere entre sujetos (colocacion del Myo, rotacion del brazalete, orden de canales), no sus momentos marginales. |
| 2026-09-04 | El commit desde el contenedor sobre el montaje deja `index.lock` que el montaje no puede borrar. Se limpiaron con permiso de borrado y el commit local `163ecb3c` se creo correctamente. | Para futuros commits: preferir `git` nativo en Windows, o limpiar `index.lock` antes de reintentar. |
| 2026-09-04 | La verificacion 5.1 de E1C detecto un error real antes de contaminar el experimento: la primera reconstruccion de ventanas rellenaba con ceros la ultima ventana de cada episodio, cuando `RecordedMyo.readEmg` devuelve una ventana parcial. Desviaciones de hasta 2.3e+01 en 5 sujetos. | CERRADO. Tras corregirlo, el recalculo en Python reproduce el export de MATLAB con error absoluto maximo 2.7e-15 sobre 940 000 celdas. |
| 2026-09-04 | Bajo desplazamiento circular de canales, F1/F4/F5/F13 solo se permutan pero `WMoos_F2` cambia de forma no trivial: aplica `hilbert` y `sgolayfilt` a lo largo del eje de canales y Savitzky-Golay no es circular. | DOCUMENTADO. Obliga a recomputar features desde la EMG cruda, no a permutar el vector exportado. |
| 2026-09-04 | Cerrada la linea de adaptacion EMG-only entre sujetos tras tres hipotesis descartadas: generalizacion directa (E1), renormalizacion afin (E1B) y alineacion espacial de canales (E1C). | La siguiente opcion a discutir es `SUPERVISED_SUBJECT_CALIBRATION`. No implementada. |
